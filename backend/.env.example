# Backend environment variables – copy to .env and fill in your values

# PostgreSQL connection
DATABASE_URL=postgresql://polyia:polyia_secret@localhost:5432/polyia_db

# JWT
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS – comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Cloud LLM providers (fill in only the ones you want to use)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-haiku-20240307

GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-1.5-flash

# Local SLM via Ollama
LOCAL_MODEL_URL=http://localhost:11434/api/generate
LOCAL_MODEL_NAME=qwen2.5:3b


# Backend environment variables – copy to .env and fill in your values

# PostgreSQL connection
DATABASE_URL=postgresql://polyia:polyia_secret@localhost:5433/polyia_db
POSTGRES_DB=polyia_db
POSTGRES_PORT=5433

# JWT
SECRET_KEY=aaaaaaaaaaaa
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS – comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Google Gemini (lesson generation)
GOOGLE_API_KEY=AIzaSyDURjO4uvGS046R6f3sATyVn6qba75y6O8
GOOGLE_MODEL=gemini-3-flash-preview

# Local SLM via Ollama
LOCAL_MODEL_URL=http://localhost:11434/api/generate
LOCAL_MODEL_NAME=qwen3:8b

