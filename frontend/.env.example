# Frontend environment variables – copy to .env and fill in your values

# If you deploy the backend to a different host, set this.
# Leave empty for local development (Vite proxy handles /api/* → localhost:8000)
VITE_API_BASE_URL=
